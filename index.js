const express = require('express');
const path = require('path');
const fs = require('fs');

const shapes = require('./shapes lib/shapes');
const PORT = 3333;

app.listen(PORT, () => console.log(`App listening on port: ${PORT}`));